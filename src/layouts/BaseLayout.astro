---
import '@styles/global.css';
import 'lite-youtube-embed/src/lite-yt-embed.css';
import Head from '@components/organisms/Head.astro';

export interface Props {
  title: string;
  description?: string;
  image?: ImageMetadata;
  lang?: string;
  class?: string;
}

const {
  title,
  description = 'Draftlab Scaffold - Our Web Development Starter',
  image,
  lang = 'en',
  class: className = '',
} = Astro.props;

const canonicalURL = Astro.site
  ? new URL(Astro.url.pathname, Astro.site)
  : Astro.url;
---

<!doctype html>
<html lang={lang}>
	<head>
		<Head
			title={title}
			description={description}
			canonicalURL={canonicalURL}
			image={image}
		>
			<slot name="head" />
		</Head>
	</head>

	<body class={className}>
		<slot />
	</body>
</html>

<script>
	const loadEmbeds = () => {
		if (document.querySelector('lite-youtube')) import('lite-youtube-embed');
		if (document.querySelector('lite-vimeo')) import('lite-vimeo-embed');
	};
	loadEmbeds();
	document.addEventListener('astro:page-load', loadEmbeds);
</script>
